node_modules/
dist/
.wrangler/
.test-dist/
*.db*
src/ui-html.ts
.claude/worktrees/
.gemini/.env
.grok/.env
# secrets never belong in the tree (a `git add -A` in the shared checkout has happened before)
.env
.env.*
**/.dev.vars
**/.dev.vars.*
*signing-key*.json
*checkpoint-key*.json
worker-credentials.json
# personal Cursor CLI allowlist — not harness identity (tokens stay in ~/.retrace/cursor.env)
.cursor/cli.json
# Copilot Chat MCP belongs in VS Code user config, not a workspace file Cursor can Start
.vscode/mcp.json
